[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
check-manifest = ">=0.25"

[packages]
flask-admin = "<2.0"
flask-security-invenio = "==4.0.0"
invenio-app-ils = {extras = ["postgresql", "opensearch2", "lorem"], version = "~=4.5.0"}
setuptools = ">=57.0.0,<58.0.0"
uwsgi = ">=2.0"
uwsgitop = ">=0.11"
uwsgi-tools = ">=1.1.1"

[requires]
python_version = "3.9"

[pipenv]
allow_prereleases = false
